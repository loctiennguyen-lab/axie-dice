class_name AnimNames
extends Object
## Typed body-animation clip names (bare — no "Default."/"Action." prefix).
## The Cannon weapon is baked as "Canon" in the source art; constants read "Cannon*" (double n).

const Dead := "Dead"
const Idle := "Idle"
const IdleCarryItem := "IdleCarryItem"
const IdleGetHit := "IdleGetHit"
const Run := "Run"
const RunAttack := "RunAttack"
const RunCarryItem := "RunCarryItem"
const Stun := "Stun"
const Walk := "Walk"
const WalkAttack := "WalkAttack"
const WalkCarryItem := "WalkCarryItem"
