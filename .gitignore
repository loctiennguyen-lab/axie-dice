# === IDE and Editor ===
.vs/
.vscode/settings.json
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
Desktop.ini

# === Claude Code Local ===
.claude/settings.local.json
CLAUDE.local.md
production/session-logs/

# === Internal Planning (not for public) ===

expansions/

# === Incoming asset drops (temp staging — useful files already copied into
#     godot/assets/ or assets/; the raw dumps here are re-downloadable from source
#     and should never be committed, see production/session-state/godot-port-active.md) ===
assets/_incoming/

# === Marrow (eval tooling — never commit) ===
marrow/

# === Runtime Artifacts (auto-generated, start empty on fresh clone) ===
# Created on first use by /consistency-check and /architecture-review
docs/consistency-failures.md

# === Build Output ===
build/
builds/
AxieDiceTactics.html
bin/
obj/
out/
dist/
*.exe
*.dll
*.so
*.dylib
*.app

# === Engine: Godot ===
.godot/
*.translation
export_presets.cfg
# Uncomment if using Godot:
# *.import

# === Engine: Unity ===
[Ll]ibrary/
[Tt]emp/
[Oo]bj/
[Bb]uild/
[Bb]uilds/
[Ll]ogs/
[Uu]ser[Ss]ettings/
*.csproj
*.unityproj
*.sln
*.suo
*.tmp
*.user
*.userprefs
*.pidb
*.booproj
*.svd
*.pdb
*.mdb
*.opendb
*.VC.db
crashlytics-buildid.txt
sysinfo.txt
# Uncomment if using Unity:
# [Aa]ssets/[Aa]sset[Ss]tore[Tt]ools/

# === Engine: Unreal ===
Binaries/
DerivedDataCache/
Intermediate/
Saved/
*.VC.opendb
*.opensdf
*.sdf
*.cachefile
*.VC.db
# Uncomment if using Unreal:
# *.uasset
# *.umap

# === Compiled Shaders ===
*.spv
*.cso
*.dxbc

# === Temporary and Cache ===
*.tmp
*.temp
*.cache
*.log
__pycache__/
*.pyc
node_modules/
.npm/

# === Archives ===
*.zip
*.tar.gz
*.rar
*.7z

# === Secrets and Environment ===
.env
.env.local
.env.*.local
*.pem
*.key
credentials.json
secrets.json

# === Large Binary Assets (use Git LFS instead) ===
# Uncomment and configure with Git LFS for your project:
# *.png
# *.jpg
# *.jpeg
# *.wav
# *.mp3
# *.ogg
# *.fbx
# *.blend
# *.psd
# *.tga
# *.exr
# *.hdr

# === OS Generated ===
ehthumbs.db
ehthumbs_vista.db
[Dd]esktop.ini
$RECYCLE.BIN/
*.cab
*.msi
*.msix
*.msm
*.msp
*.lnk
.vercel
.env*

# === Vendored upstream packages (third_party/) ===
# These are the packages as delivered by upstream, kept on disk for reference. They are NOT
# committed, for one measured reason: `godot/addons/axie_mixer_3d*` already holds the copy the
# game actually loads, and on 2026-09-19 `diff -rq` showed the two are byte-for-byte identical.
# Committing both would put ~391MB of duplicate data into history permanently — and history is
# the one thing that is painful to take back.
#
# The single exception is the golden fixture below: four gates read it
# (t_axie_gene_preview, t_axie_api, qa_vault_preview_capture, qa_vault_screen_capture), so a
# fresh clone without it has four failing tests and no obvious reason why. It is 9.9 KB.
# NOTE ON THE PATTERN SHAPE: a bare `third_party/` would make git skip the directory entirely,
# and a `!` exception under an excluded directory is never even looked at. Each level has to be
# opened with `dir/*` and then re-included, which is why this is five lines instead of one.
third_party/*
!third_party/godot-axie-mixer-3d-main/
third_party/godot-axie-mixer-3d-main/*
!third_party/godot-axie-mixer-3d-main/tests/
third_party/godot-axie-mixer-3d-main/tests/*
!third_party/godot-axie-mixer-3d-main/tests/goldens/
production/leaderboard/verdicts/
